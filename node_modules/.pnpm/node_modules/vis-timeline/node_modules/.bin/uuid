#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../uuid/dist-node/bin/uuid" "$@"
else
  exec node  "$basedir/../../../uuid/dist-node/bin/uuid" "$@"
fi
# cmd-shim-target=C:/Users/JoshuaStremmel/Who2Root4/node_modules/.pnpm/vis-timeline@8.5.1_@egjs+ha_1932c4390e0c2a4b3a293606a3ad53ec/node_modules/uuid/dist-node/bin/uuid
