#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../uuid/dist-node/bin/uuid" "$@"
else
  exec node  "$basedir/../../../uuid/dist-node/bin/uuid" "$@"
fi
# cmd-shim-target=C:/Users/JoshuaStremmel/Who2Root4/node_modules/.pnpm/vis-data@8.0.4_uuid@14.0.0__78f188d867fd657f265bc617f168d0a2/node_modules/uuid/dist-node/bin/uuid
