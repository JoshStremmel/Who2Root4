{
  "buildCommand": "pnpm --filter graph build && mkdir -p dist/ROOT4 && cp index.html dist/ && cp -r src dist/ && cp ROOT4/root4.js dist/ROOT4/",
  "outputDirectory": "dist",
  "installCommand": "git submodule update --init --recursive && pnpm install",
  "rewrites": [
    { "source": "/graph/(.*)", "destination": "/graph/index.html" },
    { "source": "/graph", "destination": "/graph/index.html" }
  ]
}
